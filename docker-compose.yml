services:
  worker:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./apps/worker:/app
    ports:
      - "8787:8787"
    command: sh -c "npm install && npm run dev -- --ip 0.0.0.0"
